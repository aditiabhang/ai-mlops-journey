apiVersion: v1
kind: Pod
metadata:
  name: config-test
spec:
  containers:
  - name: app
    image: busybox
    imagePullPolicy: Never
    command: ["sh", "-c", "echo DB=$DB_HOST ENV=$APP_ENV && sleep 3600"]
    envFrom:
    - configMapRef:
        name: app-config